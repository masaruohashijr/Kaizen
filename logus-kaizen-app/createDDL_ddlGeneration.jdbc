CREATE TABLE security.security.SEG_USUARIO (cod_usuario VARCHAR(20) NOT NULL, flg_ativo BOOLEAN NOT NULL, dsc_email VARCHAR(200) NOT NULL, nom_usuario VARCHAR(50) NOT NULL, PRIMARY KEY (cod_usuario))
